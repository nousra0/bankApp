import React from 'react'

const Button = ({styles}) => (
    
      <button type='button' className={`my-4 px-6 py-2 bg-blue-gradient font-poppins font-medium text-[18px] text-primary outline-none ${styles} rounded-[10px]`}>
        Get Started
      </button>
   
  )


export default Button
